import Section from "@/components/Section";

export default function Architecture() {
  return (
    <Section eyebrow="Architecture" title="Enterprise Architecture & Zero Trust">
      <div className="text-sm text-muted max-w-3xl leading-relaxed">
        Identity-driven security architecture for adaptive access, segmentation, and resilient deployment
        across cloud and hybrid environments. (Content coming next.)
      </div>
    </Section>
  );
}
